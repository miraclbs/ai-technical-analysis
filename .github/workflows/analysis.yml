name: Bitcoin Technical Analysis

on:
  schedule:
    # Her saat başından 1 dakika sonra çalışır (UTC saat)
    # Örnek: 00:01, 01:01, 02:01, 03:01, vb.
    - cron: '1 * * * *'
  
  # Manuel tetikleme için
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Cache Python packages
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run Advanced Analysis (Summary)
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      run: |
        echo "🔄 Gelişmiş analiz başlatılıyor (qwen3.py)..."
        python qwen3.py
        echo "✅ Gelişmiş analiz tamamlandı!"
    
    - name: Run Raw Data Collection
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      run: |
        echo "🔄 Ham veri toplama başlatılıyor (qwen3_AllData.py)..."
        python qwen3_AllData.py
        echo "✅ Ham veri toplama tamamlandı!"
    
    - name: Summary
      run: |
        echo "=========================================="
        echo "✅ Tüm analizler başarıyla tamamlandı!"
        echo "📊 btc_analysis tablosu güncellendi"
        echo "📦 btc_raw_data tablosu güncellendi"
        echo "🕐 Saat: $(date)"
        echo "=========================================="
